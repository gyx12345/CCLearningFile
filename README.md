# CCLearningFile
# CCLearningFile
1、git operator
echo "# CCLearningFile" >> README.md
git init  #初始化git repository
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/gyx12345/CCLearningFile.git  #给本地仓库，链接远程仓库
git push -u origin main   #把本地main分支 推送到远程仓库


2、acquire https keys (token)

open user navigation menu (on right member picture) -->Settings --->Developer settings (on the button of left) --->Personal access tokens --->Tokens (Classic) -->Generate new token (classic) --->check repo button --->generate
